<%= form_with(model: upvote, local: true) do |form| %>
  <% if upvote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(upvote.errors.count, "error") %> prohibited this upvote from being saved:</h2>

      <ul>
      <% upvote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :alarm_id %>
    <%= form.text_field :alarm_id, id: :upvote_alarm_id %>
  </div>

  <div class="field">
    <%= form.label :session_id %>
    <%= form.text_field :session_id, id: :upvote_session_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
